<%= form_with(model: content_provider) do |form| %>
  <% if content_provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(content_provider.errors.count, "error") %> prohibited this content_provider from being saved:</h2>

      <ul>
        <% content_provider.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :thumbnail_image %>
    <%= form.text_field :thumbnail_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
